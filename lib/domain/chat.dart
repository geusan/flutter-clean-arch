class Chat {
  int chatId;
  int writerId;
  String content;
  DateTime timestamp;
  Chat({
    required this.chatId,
    required this.writerId,
    required this.content,
    required this.timestamp,
  });
}
